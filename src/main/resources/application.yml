spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: bootsprng@gmail.com
    password: qrxu maij fsea yrqs
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            enable: true
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/balancetalk
    username: sa
    password:
  servlet:
    multipart:
      max-file-size: 3MB
      max-request-size: 5MB

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
